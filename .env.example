# Do not commit real secrets. Keep this file as placeholders only.
# External API consumption stays disabled unless you explicitly provide keys
# and set enabling flags in your local .env.

NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_READ_TOKEN=
SANITY_REVALIDATE_SECRET=
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_SITE_URL=

# Optional safety flag: keep external API disabled by default
# FEATURE_ENABLE_EXTERNAL_API=false
